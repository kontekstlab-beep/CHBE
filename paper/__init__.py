"""Paper-трейдинг mean-reversion для Binance USDT-M Futures testnet.

Замороженная стратегия (из исследования, см. корневой README):
  вход: z=(close-SMA48)/std48 < -2 -> maker-лимитка на покупку;
  выход: z>=0 (возврат к среднему) ИЛИ 8 баров ИЛИ катастроф-стоп 8%;
  сайзинг: 5% капитала на позицию; БЕЗ kill-switch (доказано, что вредит).

Модули:
  config  — замороженные параметры и корзина монет;
  signal  — расчёт z и решения (общий код с бэктестом);
  broker  — DryRunBroker (симуляция, без ключей) и TestnetBroker (ccxt sandbox);
  engine  — движок сопровождения ордеров/позиций по закрытым барам;
  state   — персистентность (JSON) для рестарт-устойчивости.
"""
