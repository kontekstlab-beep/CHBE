# Crypto trading bot — от методики ICT до mean-reversion

[![tests](https://github.com/kontekstlab-beep/CHBE/actions/workflows/tests.yml/badge.svg)](https://github.com/kontekstlab-beep/CHBE/actions/workflows/tests.yml)

Полный исследовательский цикл с честной out-of-sample-проверкой на каждом шаге:
реализация методики Smart Money / ICT → строгая валидация (edge≈0 на независимых
данных) → разведка данных → найденная закономерность **mean-reversion** (пережила
чистый HOLDOUT) → **paper-трейдинг** на Binance testnet.

Кодовая база — чистый Python (примитивы, событийный бэктестер без look-ahead,
метрики, walk-forward, мультиТФ, protocol TUNING/OOS/HOLDOUT, paper-движок). 43 теста.
Историю этапов (M1…M5) с результатами см. ниже.

## Структура

```
smartmoney/
  models.py       # датаклассы: Candle, Swing, OrderBlock, FVG, LiquidityPool, BOS
  config.py       # параметры (пороги ТЗ §14), помечены 🎓/⚙️
  structure.py    # свинги, ATR, тренд (HH/HL/LH/LL), BOS, сбор ликвидности
  imbalance.py    # FVG (имбаланс), перекрытие 50%
  liquidity.py    # пулы BSL/SSL, пометка снятия (sweep)
  orderblocks.py  # блоки заказов со score силы 0..4, митигация/инвалидция
  synth.py        # синтетические сцены для тестов/демо
tests/            # pytest на каждый примитив
demo.py           # end-to-end прогон по синтетической сцене
```

## Запуск

Интерпретатор (реальный Python 3.12, установлен через winget):
`C:\Users\konte\AppData\Local\Programs\Python\Python312\python.exe`

Тесты:
```bash
python -m pytest -q
```

Демо (UTF-8 вывод в консоль Windows):
```bash
set PYTHONUTF8=1 && set PYTHONPATH=. && python demo.py
```

## Что готово (M1 — примитивы)
- ✅ Свинги (строгие пивоты, подтверждение через N баров — без look-ahead).
- ✅ Тренд по последним хаям/лоям (up/down/range).
- ✅ BOS «телом» + отличие от сбора ликвидности (шпилька).
- ✅ FVG/имбаланс, перекрытие 50%.
- ✅ Пулы ликвидности BSL/SSL (двойные/тройные), пометка снятия.
- ✅ Блоки заказов со score 0..4; сильный = 4; митигация и инвалидция «телом».

## Что готово (M2 — стратегия и бэктест)
- ✅ Сайзинг позиции под 1% риска + подбор плеча (`sizing.py`).
- ✅ Метрики: winrate, expectancy (R), profit factor, просадка, equity (`metrics.py`).
- ✅ Событийный бэктестер: bar-by-bar, интрабар-выходы (SL первым),
     комиссии + проскальзывание, строго без look-ahead (`backtest.py`).
- ✅ Стратегия-автомат `SmartMoneyStrategy` (`strategy.py`): три масштаба свингов —
     тренд (крупные) / зона-OB (средние) / BOS-подтверждение (мелкие), что
     имитирует мультиТФ-связку §5 ТЗ на одной серии.
- ✅ Загрузка реальных свечей Binance через ccxt (`data.py`) + CSV.
- ✅ 22 юнит/интеграционных теста; движок проверен на scripted-сигналах.

### Прогон бэктеста
```bash
python backtest_demo.py                          # синтетика
python backtest_demo.py --binance SOL/USDT 1h 1000   # реальные данные (нужен ccxt)
python backtest_portfolio.py 1h 1500             # портфельно по списку монет
```

> **Статус результатов:** пороги ⚙️ НЕ калибровались. На прогоне 8 монет (1h, 1500
> свечей) движок дал 49 сделок с отрицательным матожиданием — это ожидаемо для
> некалиброванного прототипа. Цель M2 — рабочая инфраструктура, а не прибыльность.

## Что готово (M3 — калибровка)
- ✅ Ускорение движка: примитивы считаются один раз в `prepare()`, `evaluate_at(t)`
     работает «as-of» (bisect по confirmed_at) — бэктест 4000 баров ≈ 0.2 c.
- ✅ Пагинация истории Binance + CSV-кэш (`data/`) — прогоны офлайн.
- ✅ Калибратор `calibrate.py`: grid search на TRAIN, отбор по expectancy с floor по
     числу сделок, честная оценка на TEST (out-of-sample), сравнение train/test.

### Прогон калибровки
```bash
python calibrate.py     # грузит/кэширует данные, перебирает сетку, печатает train vs test
```

### Результат M3 (честный)
На 8 монетах (1h, 4000 свечей, train 65% / test 35%): лучшая комбинация на TRAIN
дала **+0.34R** (PF 1.51, 53 сделки), но на TEST — **−0.16R** (PF 0.79).
**Вывод:** налицо переобучение — устойчивого преимущества out-of-sample у прототипа
пока НЕТ. Это ожидаемо и полезно: инфраструктура калибровки корректно ловит
неробастность. Критерий приёмки ТЗ (≥100 сделок и положительное матожидание
на out-of-sample) не достигнут.

## Что готово (M3.1 — лимит-вход и безубыток)
- ✅ Лимит-вход от LTF-блока: сигнал ставит отложенную лимитку на проксимальный
     край свечи, вызвавшей слом (ретест), SL за этот блок; TTL и отмена
     (`backtest.py` — механизм `_Pending`, `is_limit` в `Signal`).
- ✅ Перевод SL в безубыток после +`breakeven_at_r`R (`RiskConfig.use_breakeven`).
- ✅ Флаги в `config.py`: `use_limit_entry`, `order_ttl_bars`, `use_breakeven`,
     `breakeven_at_r`. Движок считает исходы `tp` / `sl` / `be`.
- ✅ +3 теста (исполнение лимитки при ретесте, отмена по TTL, защита БУ). Итого 25.

### A/B out-of-sample (фиксированная комбинация, эффект каждой доработки)
| вход | безубыток | сделок | win% | expectancy | исходы |
|---|---|---|---|---|---|
| рынок | нет   | 20 | 10.0 | −0.723R | 18 SL / 2 TP |
| рынок | +1R   | 20 | 10.0 | **−0.281R** | 9 BE / 9 SL / 2 TP |
| лимит | нет   | 46 | 10.9 | −0.661R | 41 SL / 5 TP |
| лимит | +1R   | 47 |  4.3 | −0.694R | 15 BE / 30 SL / 2 TP |
| лимит | +2R   | 47 | 10.6 | −0.583R | 4 BE / 38 SL / 5 TP |

**Выводы (честно):**
- Безубыток сильно помогает при рыночном/широком стопе (−0.72R → −0.28R).
- Лимит-вход ловит вдвое больше сетапов, но с узким стопом ранний БУ (+1R) вредит —
  выбивает перед продолжением; лучше поздний БУ (+2R).
- Ни одна конфигурация НЕ прибыльна out-of-sample. Узкое место — **винрейт ~10%**
  (≈5 тейков на 46 сделок): дело не в исполнении, а в качестве точки входа/цели.

## Что готово (M3.2 — пересмотр точки входа и целей)
- ✅ Вход/стоп/цель параметризованы (`EntryConfig`): край входа
     (`proximal|mid|distal`), стоп (`block|atr`), цель (`pool_nearest|pool_farthest|rr`).
- ✅ Исследование вариантов (`explore_entry.py`): подбор на TRAIN, честная сверка
     TRAIN vs TEST по всем вариантам, затем walk-forward по 5 окнам.
- ✅ Найден устойчивый вариант → пресет `presets.backtest_v1()`.

### Ключевой результат
Прежняя логика (у-края вход, стоп за узкий блок, дальняя ликвидность RR≥3) давала
винрейт ~10% и −0.4…−0.7R. Причина — цель слишком далёкая. Пересмотр:
**глубокий вход в блок (distal) + ATR-стоп (шире, не выбивает шумом) + близкая
фикс-цель RR≈1**.

Walk-forward (5 окон, 8 монет, 1h):

| окно | сделок | винрейт | expectancy |
|---|---|---|---|
| 1 | 22 | 63.6% | +0.157R |
| 2 | 24 | 50.0% | −0.172R |
| 3 | 31 | 54.8% | +0.003R |
| 4 | 27 | 63.0% | +0.129R |
| 5 | 30 | 70.0% | +0.245R |
| **итог** | **134** | **60.4%** | **+0.076R** (плюс в 4/5 окон) |

```bash
python explore_entry.py    # сравнение вариантов + walk-forward
```

**Честные оговорки:**
- Матожидание тонкое (~+0.08R). Одно окно из пяти отрицательное.
- Цель RR≈1 **отклоняется от методики учителя** (там дальняя ликвидность, RR≥3);
  именно высокий-RR вариант в бэктесте не работал.
- Это упрощённый прототип на одной серии; комиссии/проскальзывание/реальные филлы
  могут съесть тонкий edge. На paper/реале НЕ проверено.

## Что готово (M3.3 — частичные фиксации и трейлинг)
- ✅ Движок: частичная фиксация доли позиции на +`partial_at_r`R + перевод остатка
     в безубыток + трейлинг-стоп за экстремумом ± `trail_atr_mult`·ATR. Одна позиция
     сворачивается в одну сделку с суммарным P&L; исходы `tp/sl/be/trail`.
- ✅ Флаги в `RiskConfig`: `use_partial`, `partial_at_r`, `partial_frac`,
     `use_trailing`, `trail_atr_mult`. По умолчанию выключены.
- ✅ Сравнение на walk-forward (`explore_management.py`) + пресет `backtest_v2`.
- ✅ +2 теста (точная частичка = +2R; smoke трейлинга). Итого 28.

### Ключевой результат — отрицательный (и важный)
| вариант | сделок | win% | exp,R | PF | +окон |
|---|---|---|---|---|---|
| **v1 (RR1 full)** | 134 | **60.4** | **+0.076** | 1.17 | **4/5** |
| частичка 50% | 128 | 47.7 | −0.141 | 0.76 | 2/5 |
| только трейлинг | 130 | 30.0 | −0.156 | 0.75 | 2/5 |
| частичка + трейл | 130 | 46.9 | −0.159 | 0.74 | 2/5 |

**Вывод:** попытка «дать прибыли бежать» разрушает edge. У стратегии преимущество
сосредоточено на близкой цели **RR≈1**; за её пределами движения надёжно не
продолжаются, и раннер отдаёт прибыль (винрейт 60%→47%, expectancy < 0). Это прямо
противоречит интуиции методики (цель — дальняя ликвидность), но подтверждено на
5 окнах. **Рекомендуемый пресет остаётся `backtest_v1`**; частичка/трейлинг
реализованы и доступны, но для текущего edge выключены.

## Что готово (M3.4 — настоящий мультиТФ)
- ✅ `MultiTFStrategy` (`mtf.py`): две реальные серии — HTF (4h) даёт контекст
     (тренд, блоки-зоны, ликвидность), LTF (1h) — подтверждение слома и вход.
- ✅ Строгий запрет look-ahead между ТФ: при LTF-баре t доступны только HTF-бары,
     закрывшиеся к его открытию (bisect по временам закрытия HTF).
- ✅ `build_bracket` вынесен в общую функцию (одно-ТФ и мультиТФ используют одну).
- ✅ Пресет `mtf_v1`; загрузка второго ТФ через `get_cached(sym,"4h",…)`.
- ✅ +2 теста (smoke прогон + проверка отсутствия cross-TF look-ahead). Итого 30.

### Результат (walk-forward, 5 окон, 8 монет)
| стратегия | сделок | win% | exp,R | PF | +окон |
|---|---|---|---|---|---|
| одно-ТФ v1 (1h) | 134 | 60.4 | +0.076 | 1.17 | 4/5 |
| **мультиТФ (4h→1h)** | 150 | 61.3 | **+0.102** | **1.23** | 3/5 |

**Вывод:** настоящий мультиТФ — скромное, но реальное улучшение: матожидание
+0.076→+0.102R (~+34%), profit factor 1.17→1.23, худшее окно мягче (−0.17→−0.06).
Робастность сопоставима. Пороги под мультиТФ НЕ переоптимизировались (`mtf_v1`≈`v1`),
т.е. это эффект самой архитектуры; тюнинг HTF-параметров может дать больше.

```bash
python explore_mtf.py    # одно-ТФ vs мультиТФ на walk-forward
```

## Что готово (M3.5 — тюнинг мультиТФ, связка 1D→1h) ⭐ лучший результат
- ✅ Проверена связка **1D (контекст) → 1h (вход)** + мини-тюнинг HTF-порогов
     (`explore_mtf_1d.py`), закреплена как пресет `mtf_1d_v1`.
- ✅ Кросс-секционная проверка по монетам (независимая от отбора окон).
- ✅ +1 тест. Итого 31.

### Результат (walk-forward, 5 окон, 8 монет)
| стратегия | сделок | win% | exp,R | PF | +окон |
|---|---|---|---|---|---|
| одно-ТФ v1 (1h) | 134 | 60.4 | +0.076 | 1.17 | 4/5 |
| мультиТФ 4h→1h | 150 | 61.3 | +0.102 | 1.23 | 3/5 |
| **мультиТФ 1D→1h** | 199 | 68.3 | **+0.233** | **1.65** | 3/5 |

Дневной контекст даёт заметно более чистые тренд/зоны — матожидание выросло
втрое (+0.076 → +0.233R), PF 1.17 → 1.65, винрейт до 68%. Все варианты мини-сетки
1D→1h были в плюсе, т.е. сама связка устойчиво помогает.

### Кросс-секционная проверка (по монетам, вся история) — 7/8 в плюсе
| монета | сделок | win% | exp,R |
|---|---|---|---|
| NEAR | 13 | 92.3 | +0.757 |
| CRV | 39 | 74.4 | +0.404 |
| FIL | 22 | 72.7 | +0.334 |
| SOL | 29 | 69.0 | +0.245 |
| APE | 17 | 64.7 | +0.201 |
| LINK | 29 | 65.5 | +0.170 |
| XRP | 27 | 59.3 | +0.004 |
| BNB | 21 | 52.4 | −0.184 |
| **пул** | **197** | **68.0** | **+0.227** |

```bash
python explore_mtf_1d.py    # сравнение связок + тюнинг
```

**Оговорки:** HTF-пороги подобраны на тех же данных (риск подгонки на точный порог,
хотя пул по монетам — независимая проверка). Одна монета (BNB) в минусе. Edge тонкий,
на paper/реале не проверен. Не инвестиционная рекомендация.

### Итог по edge
Рекомендуемая конфигурация — **`mtf_1d_v1` + `MultiTFStrategy(htf=1d, ltf=1h)`**:
винрейт ~68%, expectancy ~+0.23R, PF ~1.65, 7/8 монет в плюсе. Тонкое, но
статистически ненулевое преимущество на out-of-sample-выборке.

## Что готово (M3.6 — истинный out-of-sample) ⚠️ ключевой итог
- ✅ Замороженный `mtf_1d_v1` (без изменений) прогнан на **10 новых монетах**,
     которых НЕ было в тюнинге (`oos_test.py`).

### Результат: edge в основном НЕ подтвердился
| метрика | тюнинг-набор (8) | **OOS (10 новых)** |
|---|---|---|
| expectancy | +0.233R | **+0.044R** |
| profit factor | 1.65 | **1.09** |
| винрейт | 68.3% | 59.2% |
| монет в плюсе | 7/8 | **5/10** |
| плюсовых окон | 3/5 | **2/5** |

Матожидание упало на ~80%, PF схлопнулся к безубытку. По монетам разброс: GALA
+0.56R, AVAX +0.52, CHR +0.32 — но ETH −0.18, DOGE/DOT −0.12. Сильные результаты
1D→1h были в значительной мере **переобучением** под тюнинг-набор.

```bash
python oos_test.py
```

**Вывод (честный):** у стратегии в текущем виде НЕТ надёжного, переносимого на новые
инструменты преимущества. Оставшийся пул +0.044R — в пределах шума и, вероятно,
исчезнет на реальных издержках. Торговать это вживую на основании бэктеста нельзя.
Ценность этапа — истинный OOS поймал подгонку ДО реальных денег.

## Статус проекта
- **Инфраструктура готова и надёжна** (примитивы, движок без look-ahead, сайзинг,
  метрики, калибровка, walk-forward, мультиТФ, OOS-протокол) — 31 тест.
- **Устойчивый торговый edge НЕ найден.** Пайплайн честно это показал.
- Дальше имеет смысл только настоящее исследование сигнала (новые идеи входа/фильтров),
  а НЕ подгонка порогов (она переобучается — доказано). Либо признать, что метод
  в бэктесте не даёт робастного автоматизируемого преимущества.

## Что готово (M3.7 — гипотеза «фильтр режима по дневной SMA»)
- ✅ Добавлен фильтр: шорт только под HTF-SMA, лонг — над (`StructureConfig.regime_ma_len`,
     0 = выкл). Проверен по чистому протоколу: подбор на ТЮНИНГ-наборе → заморозка → OOS.

### Результат: гипотеза ОТВЕРГНУТА
| | тюнинг (walk-forward) | OOS (10 новых) |
|---|---|---|
| baseline (off) | +0.233R, 3/5 окон | +0.044R, 5/10 монет |
| regime SMA10 | +0.255R, **4/5 окон** | **−0.144R, 2/10 монет** |

На тюнинге фильтр выглядел лучше — на OOS сделал ХУЖЕ (+0.044→−0.144R). Ещё одно
подтверждение: улучшения, найденные на тюнинг-наборе, не переносятся.

```bash
python explore_regime.py
```

**Методическая оговорка:** каждая новая гипотеза, проверяемая об один и тот же OOS,
снижает его независимость (проблема множественных сравнений). Дальнейшие проверки
требуют свежего holdout-набора на каждую гипотезу.

## Общий вывод по edge
Ни базовый метод, ни протестированные доработки (лимит-вход, безубыток, частичка,
трейлинг, мультиТФ 4h/1D, фильтр режима) НЕ дали робастного, переносимого на новые
инструменты преимущества. In-sample улучшения систематически оказывались подгонкой.
**Вывод: у методики в этой реализации нет надёжного автоматизируемого edge.**
Инфраструктура (движок, метрики, walk-forward, OOS-протокол) — надёжна и переиспользуема.

## Наборы данных и протокол проверки (`smartmoney/datasets.py`)
Три НЕПЕРЕСЕКАЮЩИХСЯ набора (данные закэшированы, 1h×4000 + 1d×1000):
- **TUNING** (8): разработка/тюнинг — SOL, LINK, NEAR, FIL, APE, XRP, BNB, CRV.
- **OOS_USED** (10): первый OOS + фильтр — уже засвечен, годится как второй тюнинг.
- **HOLDOUT** (12): свежий, независимый — **ещё ни разу не использован**. BTC, TRX,
  UNI, AAVE, INJ, SUI, APT, ARB, OP, RUNE, YFI, GRT.

Протокол: тюнинг на TUNING → (опц.) сверка на OOS_USED → **ОДНА** финальная,
замороженная проверка на HOLDOUT. Каждый прогон по HOLDOUT «тратит» его
независимость — для новой пачки гипотез заводи новый свежий набор.

```bash
python validate_holdout.py            # одноразовая финальная проверка (пресет mtf_1d_v1)
python validate_holdout.py <preset>   # любой замороженный пресет
```

> HOLDOUT намеренно НЕ прогонялся при его подготовке (чтобы остаться независимым).

### Финальная проверка mtf_1d_v1 на HOLDOUT (одноразовая, заморожено)
| набор | сделок | win% | exp,R | PF | монет+ |
|---|---|---|---|---|---|
| TUNING | 199 | 68.3 | +0.233 | 1.65 | 7/8 |
| OOS_USED | 223 | 59.2 | +0.044 | 1.09 | 5/10 |
| **HOLDOUT** | 220 | 57.7 | **+0.014** | **1.03** | 8/12 |

Прогрессия +0.233 → +0.044 → +0.014R — затухание in-sample оптимизма к нулю. На
истинно независимом наборе стратегия **фактически безубыточна** (+0.014R, PF 1.03):
в пределах шума, после реальных издержек — отрицательна. По монетам разброс большой
(INJ +0.52, OP +0.34; но AAVE −0.47, YFI −0.30). Винрейт стабилен (~58%), но
R-распределение нетто около нуля.

**ОКОНЧАТЕЛЬНЫЙ ВЕРДИКТ: у методики нет торгуемого edge на независимых данных.**
HOLDOUT для конфигурации `mtf_1d_v1` теперь израсходован.

## Что готово (M4 — поиск закономерности в данных) ✅ первый OOS-живой эффект
Отказавшись от методики учителя, провёл разведку данных (`eda.py`) на TUNING:
- автокорреляция 1h-доходностей ≈ −0.02 (сильной линейной предсказуемости нет);
- НО на перепроданности стабильный **отскок к среднему** сразу по 3 метрикам
  (RSI<20, `z<−2` от SMA, падение 6ч>3%) — все указывают в одну сторону.

Собрал стратегию **mean-reversion (покупка резких проливов)** и проверил по
протоколу (свип на TUNING → заморозка → HOLDOUT). Параметры: SMA48, вход при
`z<−2`, выход при возврате к среднему или через 8ч.

### Результат — эффект ПЕРЕЖИЛ независимый holdout
| набор | сделок | win% | ср.нетто/сделку | PF | монет+ |
|---|---|---|---|---|---|
| TUNING | 524 | 53.4 | +0.326% | 1.57 | 8/8 |
| OOS_USED | 691 | 52.8 | +0.133% | 1.20 | 8/10 |
| **HOLDOUT** | 778 | 50.4 | **+0.116%** | 1.16 | 6/12 |

Впервые за проект — положительный OOS-эффект (у ICT было ~0). Валовая доходность
на holdout +0.216%/сделку.

### Но: тонкий запас над издержками (честный нюанс)
Чувствительность на HOLDOUT (round-trip):
| издержки | нетто/сделку |
|---|---|
| 0.06% (maker) | +0.156% |
| 0.10% (taker) | +0.116% |
| 0.15% | +0.066% |
| 0.20% | +0.016% |
| 0.25% | −0.034% |

Эффект держится до ~0.20% издержек. Но дип-бай торгует именно в моменты резких
проливов, где спред/проскальзывание ХУДШЕ обычного → на агрессивных филлах запас
может исчезнуть. Плюс только **6/12** монет holdout в плюсе (edge неравномерен:
ARB +0.78%, APT +0.54%, GRT +0.26% против AAVE −0.23%, TRX −0.20%).

```bash
python eda.py               # разведка структуры
python meanrev.py           # свип на TUNING
python validate_meanrev.py  # финальная проверка (вкл. HOLDOUT)
```

### Вывод по mean-reversion
Это **реальный, OOS-устойчивый эффект** — качественно лучше, чем ICT-методика.
Но edge **тонкий и чувствителен к издержкам/исполнению**, работает не на всех монетах.
Это КАНДИДАТ для доработки, а не готовая прибыльная система. Следующее:
лимит-вход (maker) чтобы срезать издержки; фильтр монет/режима; контроль хвостов
(коин, который продолжает падать); проверка на СВЕЖЕМ holdout после доработок.

## Что готово (M4.1 — maker-вход для mean-reversion)
- ✅ Модель исполнения лимитки (`meanrev_maker.py`): вход только если low ближайших
     баров коснулся лимит-уровня (иначе пропуск) — честный учёт филлов/adverse selection.

### Эффект на HOLDOUT (заморожено)
| исполнение | издержки | fill% | нетто/сделку | PF |
|---|---|---|---|---|
| taker (базовый) | 0.10% | 100 | +0.116% | 1.16 |
| maker-вход, taker-выход | 0.07% | 100 | +0.123% | 1.16 |
| **maker вход+выход** | 0.04% | 100 | **+0.153%** | 1.21 |

При лимитке на уровне close после резкого пролива **fill=100%** (следующий бар почти
всегда касается уровня) — пропусков лучших сделок нет. Maker поднял edge с +0.116%
до **+0.153%/сделку** (+32%), PF 1.16→1.21.

**Реалистичная оценка:** maker-вход достижим точно (резервная заявка). Maker-ВЫХОД
достижим для reversion-выходов (лимит-продажа на возврате к среднему = сам тезис
стратегии), но не для тайм-стопов (там taker). Значит реальный нетто — между +0.123%
и +0.153%/сделку. С учётом запаса по издержкам (edge жив до ~0.20%) это делает
mean-reversion уже приемлемым по издержкам кандидатом.

```bash
python meanrev_maker.py
```

## Итоговый статус проекта
- **Методика учителя (ICT):** честно проверена, edge на независимых данных ≈ 0.
- **Найденная закономерность:** short-term **mean-reversion** (покупка проливов) —
  единственное, что пережило чистый HOLDOUT: ~+0.12–0.15%/сделку (maker), PF ~1.2,
  win ~52%. Тонкий, но реальный edge. Не на всех монетах (6/12), чувствителен к
  издержкам/хвостам — требует фильтра монет и paper-проверки реальных филлов.
- **Инфраструктура:** примитивы, движок без look-ahead, метрики, walk-forward,
  мультиТФ, строгий TUNING/OOS/HOLDOUT-протокол, разведка данных. **37 тестов.**

## Что готово (M4.2 — фильтр монет: проверен и ОТКЛОНЁН)
Проблема: edge неравномерен (6/12 монет holdout). Дисциплина: фильтр обязан
опираться на свойство, вычислимое ЗАРАНЕЕ (нельзя оставлять монеты по их результату
на holdout — это отбор по ответу). Проверил (`coin_filter.py`):
1. **autocorr-режим** (торговать монету, пока её trailing lag-1 автокорр < 0);
2. **trailing-perf** (торговать монету, где стратегия недавно была в плюсе).

### Результат на DEV (TUNING+OOS) — фильтры не помогают
| | сделок | win% | нетто/сделку | PF |
|---|---|---|---|---|
| без фильтра | 910 | 53.2 | +0.192% | 1.30 |
| autocorr<0 | 590 | 53.6 | +0.195% | 1.33 |
| perf>0 | 369 | 55.8 | +0.201% | 1.29 |

Оба фильтра дают лишь микро-прирост нетто/сделку, но режут число сделок в 1.5–2.5×
(меньше суммарной прибыли, PF не растёт). Кросс-секционная связь автокорреляции с
прибылью слабая и обратного знака (r=+0.24) — монеты-победители НЕ предсказуемы этими
признаками. Раз на DEV пользы нет — holdout на фильтры НЕ тратился (базовая стратегия
на holdout снова подтверждена: +0.195%/сделку, PF 1.26).

**Вывод:** дисперсия по монетам похожа на шум/малую выборку на монету, а не на
разделимый заранее сигнал. Фильтр монет ОТКЛОНЁН; стратегию разумнее торговать
широкой корзиной (диверсификация), а не отбором «хороших» монет.

```bash
python coin_filter.py
```

## Что готово (M4.3 — хвостовой риск / стоп-лосс)
Проверил стоп-лосс на глубину пролива (`tail_risk.py`), метрики хвоста
(худшая сделка, 5%-хвост, волатильность, просадка). Разработка на DEV → HOLDOUT.

### HOLDOUT (заморожено)
| стоп | нетто/сделку | PF | худшая | просадка |
|---|---|---|---|---|
| нет | +0.123% | 1.16 | −8.96% | 58% |
| 2% | +0.020% | 1.03 | −2.07% | **81%** |
| 3% | +0.054% | 1.07 | −3.07% | 74% |
| 5% | +0.123% | 1.16 | −5.07% | 66% |
| 8% | +0.130% | 1.17 | −8.07% | 59% |

**Выводы (важные и против интуиции):**
1. **Тесные стопы (2–3%) ВРЕДЯТ** — режут нетто вдвое И увеличивают просадку
   (2%→81% против 58%): классическая ловушка mean-reversion — стоп выбивает перед
   отскоком, win% падает 52→47.
2. **Широкий стоп ~8% нейтрален** — кэпит худшую сделку (−12%→−8%) почти без потери
   нетто. Годится как дешёвая «страховка от катастрофы» (делистинг, обвал коина),
   но НЕ улучшает риск-профиль в целом.
3. **Просадку per-trade стоп не лечит:** она идёт от КЛАСТЕРОВ одновременных убытков
   (рынок валится → все дип-баи в минусе разом). Против коррелированного риска стоп
   бессилен.

```bash
python tail_risk.py
```

**Рекомендация:** ставить широкий катастроф-стоп (~8%) как хард-рейл, НЕ тесный.
Основной хвостовой риск — коррелированные просадки — лечится на уровне ПОРТФЕЛЯ:
лимит одновременных позиций / суммарного риска и рыночный kill-switch (пауза дип-баёв,
когда весь рынок в свободном падении), а не per-trade стопом.

## Итог по mean-reversion (после всех доработок)
- Базовый edge: **~+0.12–0.15%/сделку** на HOLDOUT (maker), PF ~1.2, win ~52–54%.
- Maker-вход снял вопрос издержек. ✅
- Фильтр монет — не помог, торговать корзиной. ✅ (вывод)
- Стоп-лосс — только широкий катастроф-стоп; риск-профиль решается на портфеле. ✅ (вывод)
- **Остаётся:** портфельные лимиты + рыночный kill-switch, затем paper на testnet.

## Что готово (M4.4 — портфельный риск и kill-switch)
Портфельный симулятор (`portfolio.py`): шаг по времени сразу по всем монетам,
общий P&L (сайзинг 5%/позицию, катастроф-стоп 8%), измерение просадки СЧЁТА.
Проверены: лимит одновременных позиций и рыночный kill-switch (по проливу за 24ч
и по медвежьему режиму «индекс < SMA200»).

### DEV — все контроли ВРЕДЯТ
| контроль | P&L | просадка счёта | P&L/DD |
|---|---|---|---|
| **без контроля** | **+12.82%** | 4.77% | **2.69** |
| лимит 6 позиций | +5.51% | 4.39% | 1.26 |
| kill −5%/24ч | +6.38% | 6.54% | 0.98 |
| kill −8%/24ч | +8.44% | 4.92% | 1.72 |
| kill медвежий (idx<SMA200) | +0.39% | 3.47% | 0.11 |
| лимит6 + kill −5% | +3.81% | 4.30% | 0.89 |

HOLDOUT: без контроля +3.87% / DD 8.35% (P&L/DD 0.46); лимит6+kill → **+0.23%** (обнулил).

**Выводы (опровергают гипотезу M4.3):**
1. Просадка СЧЁТА скромная (~5–8%) при сайзинге 5%/позицию — «58%» из M4.3 были
   артефактом суммы per-trade %, а не просадкой счёта.
2. **Любой kill-switch/лимит контрпродуктивен:** edge mean-reversion сосредоточен
   именно в падающем рынке (покупка капитуляции). Пауза «когда валится» вырезает
   лучшие сделки — медвежий-kill срезал P&L +12.8%→+0.4%.

**Правильный риск-менеджмент для этой стратегии:** маленький сайзинг (~5%/позицию) +
широкий катастроф-стоп 8% + широкая диверсификация. БЕЗ kill-switch, БЕЗ лимита позиций.

```bash
python portfolio.py
```

## Итог по mean-reversion (финал доработок)
- Edge на HOLDOUT: ~+0.12–0.15%/сделку (maker), P&L/DD ~0.46 (скромно, но положительно).
- Издержки закрыты maker-входом ✅; фильтр монет — не помог (корзина) ✅;
  стоп — только широкий катастроф ✅; портфельные контроли — вредят ✅.
- Риск-профиль уже адекватен базовым сайзингом; сложные контроли не нужны.
- **Остаётся один честный шаг:** paper на testnet — измерить РЕАЛЬНЫЕ филлы/издержки,
  единственный незакрытый вопрос для этого тонкого edge.

## Что готово (M5 — paper-трейдинг на testnet) ✅
Пакет `paper/` — mean-reversion как живое приложение. Логика сигнала общая с
бэктестом (`paper/signal.py`), три режима запуска (`run_paper.py`):

```bash
# 1) офлайн-демо (безопасно, БЕЗ ключей): реплей движка по кэшу data/
python run_paper.py --once

# 2) живой цикл на ПУБЛИЧНЫХ данных, симуляция филлов (без ключей, без ордеров)
python run_paper.py --live

# 3) живой цикл с РЕАЛЬНЫМИ ордерами на Binance TESTNET (нужны ключи)
export BINANCE_TESTNET_KEY=...      # ключи создаёте ВЫ на testnet.binancefuture.com
export BINANCE_TESTNET_SECRET=...   # права: только фьючерсы, БЕЗ вывода средств
python run_paper.py --live --testnet
```

Что делает движок (`paper/engine.py`) на каждом закрытом 1h-баре:
- z<−2 и нет позиции → **maker-лимитка** на покупку (postOnly), TTL 3 бара;
- на исполнении → лимит-продажа на среднем (SMA) как maker-выход по возврату;
- катастроф-стоп 8% и тайм-стоп 8ч → market-выход;
- сайзинг 5%/позицию; корзина 10 монет; БЕЗ kill-switch (доказано, что вредит);
- состояние в `paper_state.json` (рестарт-устойчивость), логирование, DryRun/Testnet.

Офлайн-демо (реплей 1500 баров × 10 монет) воспроизводит стратегию:
сделок 270, винрейт 56%, +0.35%/сделку — согласуется с бэктестом.

**Безопасность:** ключи только из окружения (в код не хардкодятся, я их не ввожу),
торговля только на testnet (не реальные деньги), по умолчанию — dry-run. Цель paper —
измерить РЕАЛЬНЫЕ филлы maker-лимиток против модели (единственный незакрытый вопрос
для этого тонкого edge). Гоняйте `--live --testnet` несколько недель и сравните
фактические цены/доли исполнения с бэктестом.

## Статус проекта (полный цикл)
Чат+13 видео → пересказ → ТЗ → прототип ICT (edge≈0 на OOS) → разведка данных →
**mean-reversion** (реальный OOS-edge ~+0.12–0.15%/сделку) → maker-вход (издержки
закрыты) → фильтр монет (не нужен) → стоп (только катастроф) → портфельный риск
(контроли вредят) → **paper на testnet**. 43 теста, всё офлайн-воспроизводимо.

### Известные упрощения прототипа (для M3+)
- Вход по рынку на баре подтверждения (полная версия — лимитка от LTF-блока).
- МультиТФ имитируется тремя масштабами свингов на одной серии (не отдельные ТФ).
- Нет перевода в безубыток/частичных фиксаций (каркас в ТЗ §7 есть).

> Все ⚙️-пороги в `config.py` — предварительные, требуют калибровки на бэктесте.
